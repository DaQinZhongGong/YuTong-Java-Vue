# 开发环境后端 API 基址（移动端 H5 dev 走 vite proxy: manifest.json /api -> localhost:20010）
# H5 由 request.ts 平台默认值提供 /api/v1（避免浏览器 CORS）；小程序默认直连 20010。
# 如需覆盖取消下一行注释：
# VITE_API_BASE_URL=/api/v1

# 应用版本号（我的页展示）
VITE_APP_VERSION=1.0.0-dev
