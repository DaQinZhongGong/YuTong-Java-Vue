# E2E 测试专用环境 (GA2-L183)
# 通过 vite --mode e2e 加载, 覆盖 .env.development 的 VITE_API_BASE_URL。
# 使用相对路径 /api/v1 走 vite proxy (vite.config.ts: /api -> http://localhost:20010),
# 保证 E2E 与 Docker 后端 (yutong-backend-run 20010->8080) 同源通信, 避免 CORS。
# 注: .env.development 默认值 http://localhost:8080/api/v1 指向被其他项目占用的 8080 端口,
# 故 E2E 单独覆盖; 不修改 .env.development 以免影响其他开发流程。
VITE_API_BASE_URL=/api/v1
