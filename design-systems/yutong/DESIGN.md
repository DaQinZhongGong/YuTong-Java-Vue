# DESIGN.md — yutong · 可分发设计系统包

> 与根 `DESIGN.md` 同源，此目录为 open-design 兼容的 `design-systems/<brand>/` 包结构。
> 可被 `brand-extract` / `reference-design-contract` 直接消费。

复用根 `DESIGN.md` 的 9 段式，此处补充可分发资产：

- `tokens.css` — CSS 变量完整版
- `manifest.json` — 元数据
- `components/` — 预留组件 fixture（后续由 `frontend-design` 沉淀）
