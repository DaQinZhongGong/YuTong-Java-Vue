package com.yutong.ai.harness.enums;

/**
 * 计划评审状态。AWAITING_APPROVAL → APPROVED / REVISION_REQUESTED。
 */
public enum PlanReviewState {
    DRAFT,
    AWAITING_APPROVAL,
    APPROVED,
    REVISION_REQUESTED
}
